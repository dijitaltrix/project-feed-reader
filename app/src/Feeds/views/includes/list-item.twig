<div class="feed list-item" data-id="{{ feed.id }}">
	<span class="h6"><a href="{{ path_for('feeds.view', { 'id': feed.id } ) }}">{{ feed.name }}</a></span>
	<br><small class="text-muted">{{ feed.url }}</small>
</div>
